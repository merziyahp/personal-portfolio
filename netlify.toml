[build]
  command = "cd client && npm ci && npm run build"   # or: npm install && npm run build
  publish = "client/dist"

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
